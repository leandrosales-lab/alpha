# 🚀 Guia de Deploy — GitHub Pages

## Passo 1 — Criar repositório no GitHub

1. Acesse [github.com/new](https://github.com/new)
2. Nome do repositório: `deriv-robo-system` (ou outro de sua escolha)
3. Marque **Private** (recomendado para proteger o código)
4. Clique em **Create repository**

---

## Passo 2 — Fazer upload dos arquivos

**Opção A — Via interface web (mais fácil):**
1. No repositório criado, clique em **Add file → Upload files**
2. Arraste os arquivos:
   - `deriv_system.html`
   - `INICIAR_WINDOWS.bat`
   - `README.md`
   - `.gitignore`
   - Pasta `.github/` (com o workflow)
3. Clique em **Commit changes**

**Opção B — Via terminal (Claude Code):**
```bash
git init
git add .
git commit -m "🚀 Deploy inicial do Deriv Robo System"
git branch -M main
git remote add origin https://github.com/SEU-USUARIO/SEU-REPO.git
git push -u origin main
```

---

## Passo 3 — Ativar GitHub Pages

1. No repositório, vá em **Settings → Pages**
2. Em **Source**, selecione **GitHub Actions**
3. Salve

O deploy acontece automaticamente a cada `git push` na branch `main`.

---

## Passo 4 — Acessar o sistema online

Após o primeiro deploy (1-2 minutos), seu sistema estará em:

```
https://SEU-USUARIO.github.io/SEU-REPO/deriv_system.html
```

Substitua `SEU-USUARIO` e `SEU-REPO` pelos seus dados reais.

---

## Passo 5 — Atualizar o sistema

Sempre que receber uma nova versão do `deriv_system.html`:

```bash
# Substituir o arquivo e fazer push
git add deriv_system.html
git commit -m "✨ Atualização do sistema"
git push
```

O GitHub Actions faz o deploy automaticamente em ~1 minuto.

---

## 🔒 Dica de segurança

O repositório **Private** esconde o código-fonte mas o GitHub Pages
serve o HTML publicamente. Isso é o comportamento correto — o cliente
acessa o sistema pela URL mas não vê o repositório.

Para controle de acesso, use a aba **🎨 Marca** no admin para configurar
seus dados de contato e distribua a URL apenas para quem pagou.

---

## 📞 Fluxo de venda sugerido

```
Cliente interessa → você envia a URL → ele testa em DEMO
→ decide comprar → você recebe via Pix
→ ele continua usando (conta real, token próprio)
→ fim do mês → você pede print do histórico Deriv
→ cobra % do lucro
```
